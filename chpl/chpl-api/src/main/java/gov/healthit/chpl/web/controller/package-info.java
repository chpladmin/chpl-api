/**
 * The main way the user interface interacts with the API is through the URLs
 * mapped in controllers. Typically, the controller validates any parts of the
 * request it is interested and passes on much of the work to a manager.
 */
package gov.healthit.chpl.web.controller;
