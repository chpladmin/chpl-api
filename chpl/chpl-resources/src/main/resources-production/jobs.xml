<?xml version="1.0" encoding="utf-8"?>
<job-scheduling-data
    xmlns="http://www.quartz-scheduler.org/xml/JobSchedulingData"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.quartz-scheduler.org/xml/JobSchedulingData http://www.quartz-scheduler.org/xml/job_scheduling_data_2_0.xsd"
    version="2.0">
    <pre-processing-commands>
        <delete-triggers-in-group>triggerJob</delete-triggers-in-group>
        <delete-triggers-in-group>interruptJob</delete-triggers-in-group>
        <!-- Delete the below pre-processing command(s) after PROD push -->
        <!-- End delete pre-processing command(s). -->
    </pre-processing-commands>
    <schedule>
        <!-- Delete from here to "DELETE END MARKER" after PROD push -->
        <job>
            <name>insertMissingSplitActivityJob</name>
            <group>systemJobs</group>
            <description>Add split activity for developers and products that was missing.</description>
            <job-class>gov.healthit.chpl.scheduler.job.InsertMissingSplitActivityJob</job-class>
            <durability>true</durability>
            <recover>false</recover>
        </job>
        <!-- DELETE END MARKER -->
    </schedule>
</job-scheduling-data>
